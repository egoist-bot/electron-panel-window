import { BrowserWindow } from "electron"

export const makeKeyWindow: (window: BrowserWindow) => void
export const makePanel: (window: BrowserWindow) => void
export const makeWindow: (window: BrowserWindow) => void
